node_modules
dist

# Vite loads .env, .env.local, .env.production, .env.development and their
# .local variants — ignore them all, keep the checked-in example.
.env*
!.env.example

*.log

.vercel

# generated by scripts/daily-report.mjs
report.html
shots/
review-build.html

# scripts/capture-evidence.mjs + capture-gallery.mjs — screenshots and video,
# regenerated on demand and far too large to track
evidence/
.shots.json

# Intermediate render source for the PDFs in reports/ — the PDF is the artifact.
reports/*.html
