{
  "name": "nexus-radar",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "description": "Nexus AI radar — self-updating tool database pipeline (scout → enrich → validate → publish → course-gen → skills). Host-agnostic Node ESM; no framework.",
  "scripts": {
    "start": "node run.js",
    "dry-run": "node run.js --dry-run",
    "seed": "node seed.js",
    "sync": "node scripts/sync-to-app.js",
    "test": "node --test"
  }
}
